# Linux Day 7 - Package Management

## Goal

Learn basic package management with `apt`.

## Commands

```bash
sudo apt update
sudo apt upgrade
sudo apt install tree -y
sudo apt remove tree
sudo apt purge tree
apt search tree
apt list --installed | grep tree